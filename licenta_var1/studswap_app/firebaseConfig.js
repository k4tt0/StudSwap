// Firebase configuration for web (if needed for client-side operations in future)
// For now, all Firebase operations go through the backend server

export const API_BASE_URL = 'http://192.168.100.149:5000';
